package com.google.foobar.bunny;

/**
 * Level 2.
 */
public class Solution {

    public static String solution(long x, long y) {
        return Long.toString(x * x / 2 - x / 2 + x * y + y * y / 2 - 3 * y / 2 + 1);
    }

}
